### .pio\libdeps\esp32\UDCP\src\UDCP.cpp```#include "UDCP.h"UDCP *UDCP::_instance = nullptr;UDCP::UDCP(){    _instance = this;}UDCP::~UDCP(){    stop();}void UDCP::begin(const UDCPConfig &config){    _cfg = config;    _startAP();    if (_cfg.httpEnabled)        _startHTTP();    if (_cfg.wsEnabled)        _startWS();    if (_cfg.udpEnabled)        _startUDP();    _emitEvent(UDCPEventType::SERVER_STARTED, "UDCP ready on " + getIP());}void UDCP::loop(){    unsigned long now = millis();    if (now - _lastUptimeMs >= 1000)    {        _uptimeSeconds++;        _lastUptimeMs = now;    }    if (_httpRunning && _http)        _http->handleClient();    if (_wsRunning && _ws)        _ws->loop();    if (_udpRunning)        _loopUDP();}void UDCP::stop(){    _stopHTTP();    _stopWS();    _stopUDP();    WiFi.softAPdisconnect(true);    _emitEvent(UDCPEventType::SERVER_STOPPED, "All servers stopped");}void UDCP::enableHTTP(bool enable){    if (enable && !_httpRunning)        _startHTTP();    if (!enable && _httpRunning)        _stopHTTP();}void UDCP::enableWS(bool enable){    if (enable && !_wsRunning)        _startWS();    if (!enable && _wsRunning)        _stopWS();}void UDCP::enableUDP(bool enable){    if (enable && !_udpRunning)        _startUDP();    if (!enable && _udpRunning)        _stopUDP();}void UDCP::route(const String &path, int method, HTTPHandler handler){    if (!_http)        return;    _http->on(path.c_str(), (HTTPMethod)method, [this, handler]()              {        _applyCORSHeaders();        handler(); });}void UDCP::routeNotFound(HTTPHandler handler){    if (!_http)        return;    _http->onNotFound([this, handler]()                      {        _applyCORSHeaders();        handler(); });}void UDCP::httpSend(int code, const String &contentType,                    const String &body){    if (_http)        _http->send(code, contentType.c_str(), body);}void UDCP::httpSendHeader(const String &name, const String &value){    if (_http)        _http->sendHeader(name, value);}bool UDCP::httpHasArg(const String &name){    if (!_http)        return false;    return _http->hasArg(name);}String UDCP::httpArg(const String &name){    if (!_http)        return "";    return _http->arg(name);}String UDCP::httpMethod(){    if (!_http)        return "";    switch (_http->method())    {    case HTTP_GET:        return "GET";    case HTTP_POST:        return "POST";    case HTTP_PUT:        return "PUT";    case HTTP_DELETE:        return "DELETE";    case HTTP_PATCH:        return "PATCH";    default:        return "UNKNOWN";    }}void UDCP::send(const String &message, UDCPProtocol protocol){    switch (protocol)    {    case UDCPProtocol::WS:    {        if (_wsRunning && _ws)        {            String tmp = message;            _ws->broadcastTXT(tmp);        }        break;    }    case UDCPProtocol::UDP:        if (_udpRunning && _lastUdpSenderPort != 0)            sendUDPTo(message, _lastUdpSenderIP, _lastUdpSenderPort);        break;    default:        break;    }}void UDCP::sendTo(const String &message, uint8_t wsClientId){    if (_wsRunning && _ws)    {        String tmp = message;        _ws->sendTXT(wsClientId, tmp);    }}void UDCP::broadcast(const String &message){    if (_wsRunning && _ws)    {        String tmp = message;        _ws->broadcastTXT(tmp);    }    if (_udpRunning && _lastUdpSenderPort != 0)        sendUDPTo(message, _lastUdpSenderIP, _lastUdpSenderPort);}void UDCP::broadcastExcept(const String &message, uint8_t excludeId){    if (!_wsRunning || !_ws)        return;    for (uint8_t i = 0; i < 5; i++)    {        if (i != excludeId &&            _ws->remoteIP(i) != IPAddress(0, 0, 0, 0))        {            String tmp = message;            _ws->sendTXT(i, tmp);        }    }}void UDCP::sendUDPTo(const String &message, const IPAddress &ip, uint16_t port){    if (!_udpRunning)        return;    _udp.beginPacket(ip, port);    _udp.print(message);    _udp.endPacket();}void UDCP::onPacket(OnPacketCallback cb) { _onPacket = cb; }void UDCP::onClientConnect(OnClientCallback cb) { _onConnect = cb; }void UDCP::onClientDisconnect(OnClientCallback cb) { _onDisconnect = cb; }void UDCP::onEvent(OnEventCallback cb) { _onEvent = cb; }void UDCP::onRaw(OnRawCallback cb) { _onRaw = cb; }String UDCP::getIP() const { return WiFi.softAPIP().toString(); }int UDCP::getClientCount() const { return WiFi.softAPgetStationNum(); }uint32_t UDCP::getFreeHeap() const { return ESP.getFreeHeap(); }unsigned long UDCP::getUptime() const { return _uptimeSeconds; }bool UDCP::isHTTPRunning() const { return _httpRunning; }bool UDCP::isWSRunning() const { return _wsRunning; }bool UDCP::isUDPRunning() const { return _udpRunning; }String UDCP::getDeviceId() const { return String(_cfg.deviceId); }IPAddress UDCP::getLastUDPSenderIP() const { return _lastUdpSenderIP; }uint16_t UDCP::getLastUDPSenderPort() const { return _lastUdpSenderPort; }String UDCP::buildStatusJSON() const{    StaticJsonDocument<256> doc;    doc["deviceId"] = _cfg.deviceId;    doc["ip"] = getIP();    doc["uptime"] = _uptimeSeconds;    doc["freeHeap"] = getFreeHeap();    doc["clients"] = getClientCount();    doc["http"] = _httpRunning;    doc["ws"] = _wsRunning;    doc["udp"] = _udpRunning;    String out;    serializeJson(doc, out);    return out;}void UDCP::_startAP(){    WiFi.mode(WIFI_AP);    WiFi.softAP(        _cfg.apSSID,        strlen(_cfg.apPassword) > 0 ? _cfg.apPassword : nullptr,        _cfg.apChannel, 0, _cfg.apMaxConn);    IPAddress ip(_cfg.apIP[0], _cfg.apIP[1],                 _cfg.apIP[2], _cfg.apIP[3]);    WiFi.softAPConfig(ip, ip, IPAddress(255, 255, 255, 0));    delay(300);}void UDCP::_startHTTP(){    if (_httpRunning)        return;    _http = new WebServer(_cfg.httpPort);    _registerBuiltinRoutes();    _http->begin();    _httpRunning = true;    _emitEvent(UDCPEventType::SERVER_STARTED,               "HTTP on port " + String(_cfg.httpPort));}void UDCP::_stopHTTP(){    if (!_httpRunning || !_http)        return;    _http->stop();    delete _http;    _http = nullptr;    _httpRunning = false;    _emitEvent(UDCPEventType::SERVER_STOPPED, "HTTP stopped");}void UDCP::_registerBuiltinRoutes(){    _http->on("/message", HTTP_OPTIONS, [this]()              {        _applyCORSHeaders();        _http->send(204); });    _http->on("/status", HTTP_OPTIONS, [this]()              {        _applyCORSHeaders();        _http->send(204); });    _http->on("/message", HTTP_POST, [this]()              {        _applyCORSHeaders();        if (!_http->hasArg("plain")) {            _http->send(400, "application/json", "{\"error\":\"empty_body\"}");            return;        }        String body = _http->arg("plain");        if (_onRaw) _onRaw(body, UDCPProtocol::HTTP);        _processPacket(body, UDCPProtocol::HTTP);        _http->send(200, "application/json", "{\"status\":\"ok\"}"); });    _http->on("/status", HTTP_GET, [this]()              {        _applyCORSHeaders();        _http->send(200, "application/json", buildStatusJSON()); });    _http->on("/udcp/ws/enable", HTTP_POST, [this]()              {        _applyCORSHeaders();        enableWS(true);        _http->send(200, "application/json", "{\"ws\":true}"); });    _http->on("/udcp/ws/disable", HTTP_POST, [this]()              {        _applyCORSHeaders();        enableWS(false);        _http->send(200, "application/json", "{\"ws\":false}"); });    _http->on("/udcp/udp/enable", HTTP_POST, [this]()              {        _applyCORSHeaders();        enableUDP(true);        _http->send(200, "application/json", "{\"udp\":true}"); });    _http->on("/udcp/udp/disable", HTTP_POST, [this]()              {        _applyCORSHeaders();        enableUDP(false);        _http->send(200, "application/json", "{\"udp\":false}"); });}void UDCP::_applyCORSHeaders(){    if (!_http)        return;    _http->sendHeader("Access-Control-Allow-Origin", "*");    _http->sendHeader("Access-Control-Allow-Methods",                      "GET,POST,PUT,DELETE,PATCH,OPTIONS");    _http->sendHeader("Access-Control-Allow-Headers",                      "Content-Type,Authorization");}void UDCP::_startWS(){    if (_wsRunning)        return;    _ws = new WebSocketsServer(_cfg.wsPort);    _ws->begin();    _ws->onEvent(_wsEventHandler);    _wsRunning = true;    _emitEvent(UDCPEventType::SERVER_STARTED,               "WS on port " + String(_cfg.wsPort));}void UDCP::_stopWS(){    if (!_wsRunning || !_ws)        return;    _ws->close();    delete _ws;    _ws = nullptr;    _wsRunning = false;    _emitEvent(UDCPEventType::SERVER_STOPPED, "WS stopped");}void UDCP::_wsEventHandler(uint8_t num, WStype_t type,                           uint8_t *payload, size_t length){    if (!_instance)        return;    switch (type)    {    case WStype_CONNECTED:    {        IPAddress ip = _instance->_ws->remoteIP(num);        UDCPClientInfo info{num, ip.toString(), UDCPProtocol::WS};        _instance->_emitEvent(UDCPEventType::CLIENT_CONNECTED,                              ip.toString());        if (_instance->_onConnect)            _instance->_onConnect(info);        break;    }    case WStype_DISCONNECTED:    {        UDCPClientInfo info{num, "", UDCPProtocol::WS};        _instance->_emitEvent(UDCPEventType::CLIENT_DISCONNECTED,                              "client #" + String(num));        if (_instance->_onDisconnect)            _instance->_onDisconnect(info);        break;    }    case WStype_TEXT:    {        String pkt = String((char *)payload);        if (_instance->_onRaw)            _instance->_onRaw(pkt, UDCPProtocol::WS);        _instance->_processPacket(pkt, UDCPProtocol::WS);        break;    }    default:        break;    }}void UDCP::_startUDP(){    if (_udpRunning)        return;    _udp.begin(_cfg.udpPort);    _udpRunning = true;    _emitEvent(UDCPEventType::SERVER_STARTED,               "UDP on port " + String(_cfg.udpPort));}void UDCP::_stopUDP(){    if (!_udpRunning)        return;    _udp.stop();    _udpRunning = false;    _emitEvent(UDCPEventType::SERVER_STOPPED, "UDP stopped");}void UDCP::_loopUDP(){    int pktSize = _udp.parsePacket();    if (!pktSize)        return;    _lastUdpSenderIP = _udp.remoteIP();    _lastUdpSenderPort = _udp.remotePort();    char *buf = new char[pktSize + 1];    int len = _udp.read(buf, pktSize);    if (len > 0)        buf[len] = '\0';    String payload = String(buf);    delete[] buf;    if (_onRaw)        _onRaw(payload, UDCPProtocol::UDP);    _processPacket(payload, UDCPProtocol::UDP);}void UDCP::_processPacket(const String &raw, UDCPProtocol protocol){    StaticJsonDocument<2048> doc;    if (deserializeJson(doc, raw))    {        _emitEvent(UDCPEventType::ERROR, "JSON parse failed");        return;    }    UDCPPacket pkt;    pkt.protocol = _protocolName(protocol);    pkt.v = doc["v"] | 1;    pkt.msgId = doc["msgId"] | "";    pkt.ts = doc["ts"] | 0UL;    pkt.type = doc["type"] | "";    pkt.name = doc["name"] | "";    pkt.seq = doc["seq"] | 0;    pkt.streamId = doc["streamId"] | "";    pkt.qos = doc["qos"] | 0;    String token = "", sig = "";    JsonObject auth = doc["auth"];    if (!auth.isNull())    {        pkt.authId = auth["id"] | "";        token = auth["token"] | "";        pkt.nonce = auth["nonce"] | "";        pkt.authTs = auth["ts"] | 0UL;        pkt.authExp = auth["exp"] | 0UL;        sig = auth["sig"] | "";    }    if (strlen(_cfg.authToken) > 0 && !_verifyToken(token))    {        _emitEvent(UDCPEventType::AUTH_FAILED, pkt.msgId);        return;    }    if (sig.length() > 0 && !_verifyHMAC(doc, sig, pkt))    {        _emitEvent(UDCPEventType::HMAC_FAILED, pkt.msgId);        return;    }    JsonObject val = doc["value"];    if (!val.isNull())    {        pkt.action = val["action"] | "";        pkt.value = val;    }    _dispatchPacket(pkt, doc);}void UDCP::_dispatchPacket(UDCPPacket &pkt, JsonDocument &doc){    _emitEvent(UDCPEventType::PACKET_RECEIVED,               pkt.name + " [" + pkt.protocol + "]");    if (_onPacket)        _onPacket(pkt);    if (pkt.qos > 0)    {        UDCPProtocol proto =            pkt.protocol == "HTTP" ? UDCPProtocol::HTTP : pkt.protocol == "WS" ? UDCPProtocol::WS                                                                               : UDCPProtocol::UDP;        _sendAck(pkt.msgId, proto);    }}void UDCP::_sendAck(const String &msgId, UDCPProtocol protocol){    String ack = "{\"status\":\"ACK\","                 "\"resolved\":\"" +                 msgId + "\","                         "\"device\":\"" +                 String(_cfg.deviceId) + "\"}";    send(ack, protocol);}bool UDCP::_verifyToken(const String &token){    return token == String(_cfg.authToken);}bool UDCP::_verifyHMAC(JsonDocument &doc, const String &sig,                       const UDCPPacket &pkt){    String route = _sortedRoute(doc);    String canonical = _buildCanonical(pkt, route);    String computed = _computeHMAC(canonical, String(_cfg.authSecret));    return sig == computed;}String UDCP::_buildCanonical(const UDCPPacket &pkt,                             const String &sortedRoute){    return String(pkt.v) + "|" + pkt.msgId + "|" +           String(pkt.ts) + "|" + pkt.type + "|" +           pkt.name + "|" + String(pkt.seq) + "|" +           pkt.streamId + "|" + String(pkt.qos) + "|" +           sortedRoute + "|" + pkt.authId + "|" +           pkt.nonce + "|" + String(pkt.authTs) + "|" +           String(pkt.authExp);}String UDCP::_sortedRoute(JsonDocument &doc){    JsonArray arr = doc["route"];    if (arr.isNull() || arr.size() == 0)        return "";    int sz = arr.size();    String *routes = new String[sz];    for (int i = 0; i < sz; i++)        routes[i] = arr[i].as<String>();    for (int i = 0; i < sz - 1; i++)        for (int j = 0; j < sz - i - 1; j++)            if (routes[j] > routes[j + 1])            {                String t = routes[j];                routes[j] = routes[j + 1];                routes[j + 1] = t;            }    String result = "";    for (int i = 0; i < sz; i++)    {        result += routes[i];        if (i < sz - 1)            result += ",";    }    delete[] routes;    return result;}String UDCP::_computeHMAC(const String &message, const String &key){    byte hmac[32];    mbedtls_md_context_t ctx;    mbedtls_md_init(&ctx);    mbedtls_md_setup(&ctx,                     mbedtls_md_info_from_type(MBEDTLS_MD_SHA256), 1);    mbedtls_md_hmac_starts(&ctx,                           (const unsigned char *)key.c_str(), key.length());    mbedtls_md_hmac_update(&ctx,                           (const unsigned char *)message.c_str(), message.length());    mbedtls_md_hmac_finish(&ctx, hmac);    mbedtls_md_free(&ctx);    String result = "";    for (int i = 0; i < 32; i++)    {        char b[3];        sprintf(b, "%02x", hmac[i]);        result += b;    }    return result;}String UDCP::_protocolName(UDCPProtocol p) const{    switch (p)    {    case UDCPProtocol::HTTP:        return "HTTP";    case UDCPProtocol::WS:        return "WS";    case UDCPProtocol::UDP:        return "UDP";    default:        return "UNKNOWN";    }}void UDCP::_emitEvent(UDCPEventType type, const String &detail){    if (_onEvent)        _onEvent(type, detail);}```### .pio\libdeps\esp32\UDCP\src\UDCP.h```#pragma once#include <functional>#include <WiFi.h>#include <WebServer.h>#include <WiFiUdp.h>#include <ArduinoJson.h>#include <WebSocketsServer.h>#include "mbedtls/md.h"#include "UDCPConfig.h"#include "UDCPEvents.h"class UDCP{public:    UDCP();    ~UDCP();    void begin(const UDCPConfig &config);    void loop();    void stop();    void enableHTTP(bool enable);    void enableWS(bool enable);    void enableUDP(bool enable);    void route(const String &path, int method, HTTPHandler handler);    void routeNotFound(HTTPHandler handler);    void httpSend(int code, const String &contentType, const String &body);    void httpSendHeader(const String &name, const String &value);    bool httpHasArg(const String &name);    String httpArg(const String &name);    String httpMethod();    void send(const String &message, UDCPProtocol protocol);    void sendTo(const String &message, uint8_t wsClientId);    void broadcast(const String &message);    void broadcastExcept(const String &message, uint8_t excludeId);    void sendUDPTo(const String &message, const IPAddress &ip, uint16_t port);    void onPacket(OnPacketCallback cb);    void onClientConnect(OnClientCallback cb);    void onClientDisconnect(OnClientCallback cb);    void onEvent(OnEventCallback cb);    void onRaw(OnRawCallback cb);    String getIP() const;    int getClientCount() const;    uint32_t getFreeHeap() const;    unsigned long getUptime() const;    bool isHTTPRunning() const;    bool isWSRunning() const;    bool isUDPRunning() const;    String getDeviceId() const;    String buildStatusJSON() const;    IPAddress getLastUDPSenderIP() const;    uint16_t getLastUDPSenderPort() const;private:    UDCPConfig _cfg;    WebServer *_http = nullptr;    WebSocketsServer *_ws = nullptr;    WiFiUDP _udp;    bool _httpRunning = false;    bool _wsRunning = false;    bool _udpRunning = false;    unsigned long _uptimeSeconds = 0;    unsigned long _lastUptimeMs = 0;    IPAddress _lastUdpSenderIP;    uint16_t _lastUdpSenderPort = 0;    OnPacketCallback _onPacket = nullptr;    OnClientCallback _onConnect = nullptr;    OnClientCallback _onDisconnect = nullptr;    OnEventCallback _onEvent = nullptr;    OnRawCallback _onRaw = nullptr;    void _startAP();    void _startHTTP();    void _stopHTTP();    void _startWS();    void _stopWS();    void _startUDP();    void _stopUDP();    void _loopUDP();    void _registerBuiltinRoutes();    void _applyCORSHeaders();    void _processPacket(const String &raw, UDCPProtocol protocol);    void _dispatchPacket(UDCPPacket &packet, JsonDocument &doc);    void _sendAck(const String &msgId, UDCPProtocol protocol);    bool _verifyToken(const String &token);    bool _verifyHMAC(JsonDocument &doc, const String &sig, const UDCPPacket &pkt);    String _computeHMAC(const String &message, const String &key);    String _buildCanonical(const UDCPPacket &pkt, const String &sortedRoute);    String _sortedRoute(JsonDocument &doc);    String _protocolName(UDCPProtocol p) const;    void _emitEvent(UDCPEventType type, const String &detail = "");    static UDCP *_instance;    static void _wsEventHandler(uint8_t num, WStype_t type, uint8_t *payload, size_t length);};```### .pio\libdeps\esp32\UDCP\src\UDCPConfig.h```#pragma once#include <stdint.h>struct UDCPConfig{    const char *apSSID = "UDCP-Device";    const char *apPassword = "";    uint8_t apChannel = 1;    uint8_t apMaxConn = 4;    uint8_t apIP[4] = {192, 168, 4, 1};    bool httpEnabled = true;    int httpPort = 3000;    bool wsEnabled = true;    int wsPort = 8080;    bool udpEnabled = true;    int udpPort = 9000;    const char *authToken = "";    const char *authSecret = "";    const char *deviceId = "udcp-device-01";};```### .pio\libdeps\esp32\UDCP\src\UDCPEvents.h```#pragma once#include <Arduino.h>#include <ArduinoJson.h>#include <functional>enum class UDCPProtocol{    HTTP,    WS,    UDP};enum class UDCPEventType{    CLIENT_CONNECTED,    CLIENT_DISCONNECTED,    PACKET_RECEIVED,    PACKET_REJECTED,    AUTH_FAILED,    HMAC_FAILED,    SERVER_STARTED,    SERVER_STOPPED,    ERROR};struct UDCPClientInfo{    uint8_t id;    String ip;    UDCPProtocol protocol;};struct UDCPPacket{    int v = 1;    String msgId = "";    unsigned long ts = 0;    String type = "";    String name = "";    int seq = 0;    String streamId = "";    int qos = 0;    String action = "";    String protocol = "";    JsonObject value;    String authId = "";    String nonce = "";    unsigned long authTs = 0;    unsigned long authExp = 0;};using OnPacketCallback = std::function<void(const UDCPPacket &)>;using OnClientCallback = std::function<void(const UDCPClientInfo &)>;using OnEventCallback = std::function<void(UDCPEventType, const String &)>;using OnRawCallback = std::function<void(const String &, UDCPProtocol)>;using HTTPHandler = std::function<void()>;```